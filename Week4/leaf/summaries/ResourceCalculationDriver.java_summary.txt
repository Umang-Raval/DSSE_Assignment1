Here is a merged, coherent, semantic file-level summary:

**File-Level Summary: Resource Management for Hierarchical Queues in Hadoop YARN Environment**

**Architectural Role:**
* **Components:** 
	1. `ResourceCalculationDriver` (Scheduler/Resource Management Layer)
	2. `QueueResourceManager` (Business Logic/Domain Layer, likely part of a larger Resource Management or Scheduling system)
* **Shared Responsibility:** Jointly manage and calculate resource allocations for hierarchical queues, ensuring efficient utilization within a Hadoop YARN environment.

**Key Functionality:**
* **Resource Calculation:**
	+ Drive resource calculations for child queues under a parent queue based on absolute, percentage, or weighted capacities.
	+ Determine minimum and maximum effective resources for child queues.
* **Resource Validation and Alerting:**
	+ Validate queue resource allocations (min/max resources) within the hierarchical structure.
	+ Generate warnings for resource overutilization, underutilization, and edge cases (e.g., legacy auto-queues, zero-resource scenarios).

**Core Logic Highlights:**
* Initialize remaining resources from the parent queue's available resources.
* Iterate through calculators, resources, and child queues to calculate effective resources.
* Apply business logic rules for legacy auto-queues, resource over/underutilization, and zero-resource scenarios.
* Update remaining resources after each calculator phase and generate relevant warnings.

**Unified Inputs and Outputs:**
* **Inputs:**
	+ `CSQueue` (parent queue)
	+ `QueueCapacityUpdateContext` (update context)
	+ `Map<ResourceUnitCapacityType, AbstractQueueCapacityCalculator>` (calculators)
	+ `Collection<String>` (defined resources)
	+ `context` (queue context, including queue path and resource name)
	+ `parentMaximumResource`
	+ `remainingResourceUnderParent`
	+ `queue` (queue object, potentially with configured node labels)
* **Outputs:**
	+ Calculated minimum and maximum effective resources for child queues
	+ Updated remaining resources for the parent queue
	+ `ImmutablePair<minimumResource, maximumResource>`
	+ Update warnings added to `updateContext`

**Consolidated Dependencies:**
* `org.apache.hadoop.yarn.server.resourcemanager.scheduler.capacity` package
* `org.apache.commons.lang3.tuple` for pair and immutable pair implementations
* `java.util` collections (e.g., `Map`, `Collection`)
* `QueueUpdateWarningType` enum
* `ImmutablePair`