Below is a merged, coherent, semantic file-level summary of the provided Java source code chunks, synthesizing key aspects into a unified overview:

**Unified Summary**

* **Architectural Role:**
	+ **Component:** Unified Users and Resource Manager (UURM)
	+ **Subsystem:** Resource Management and Scheduling (within a larger Distributed System, likely Apache Hadoop YARN)
	+ **Responsibility:** Centrally manages user-specific resources, tracks usage, enforces limits, and influences scheduling decisions

* **Key Functionality:**
	+ Tracks users and their resource usage within queues
	+ Enforces user-defined limits on resource consumption
	+ Provides APIs for adding, removing, and querying users
	+ Maintains statistics on user activity and resource utilization
	+ Calculates user resource limits based on queue capacity, user weights, and scheduling modes
	+ Updates and maintains user resource usage tracking (active vs. non-active users)
	+ Handles user application activation/deactivation, adjusting resource allocations

* **Core Logic:**
	- **User Management:**
		- Atomically adds/removes users from the system
		- Updates user resource limits and usage
	- **Resource Utilization Tracking:**
		- Monitors and updates resource usage for each user
		- Calculates usage ratios for efficient resource allocation
	- **Limit Enforcement:**
		- Periodically recomputes user limits based on system state changes
	- **Resource Calculation:**
		- Determines user limit resources considering current capacity, active users, and user weights
		- Applies capping based on maximum user limit (dependent on scheduling mode)

* **Inputs and Outputs:**
	- **Inputs:**
		- `QueueMetrics` (queue performance data)
		- `AbstractLeafQueue` (queue configuration)
		- `RMNodeLabelsManager` (node labels for resource management)
		- `ResourceCalculator` (for calculating resource ratios)
		- User additions/removals, updates to user resource limits
		- `userName`, `applicationId`, `resource`, `clusterResource`, `nodePartition`
		- System configurations (e.g., scheduling mode, queue capacities, user weights)
	- **Outputs:**
		- User objects with updated resource usage and limits
		- Aggregate statistics on user activity and resource utilization
		- Calculated `userLimitResource`
		- Updated user resource usage tracking
		